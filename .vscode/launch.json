{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Docker .NET Core Attach (Preview)",
            "type": "docker",
            "request": "attach",
            "platform": "netCore",
            "processName": "Lofi.API",
            "sourceFileMap": {
                "/src": "${workspaceFolder}/Lofi.API"
            }
        }
    ]
}